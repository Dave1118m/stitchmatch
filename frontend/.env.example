# ==========================================
# StitchMatch Frontend Environment Template
# ==========================================

# Leave blank if using Nginx reverse proxy on the same domain (relative /api)
# Or set to your hosted backend URL if deploying frontend to Vercel/Netlify:
# VITE_API_URL=https://stitchmatch-api.onrender.com
VITE_API_URL=
